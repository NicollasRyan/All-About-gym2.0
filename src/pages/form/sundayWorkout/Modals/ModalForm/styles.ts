import styled from "@emotion/styled";
import { Box, Button } from "@mui/material";

export const BoxModal = styled(Box)`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 400px;
  border-radius: 10px;
  background-color: #fff;
  padding: 16px;
`;

export const ButtonTraning = styled(Button)``;
